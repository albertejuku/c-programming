#include<stdio.h>

int main(){
	/*
		There are 8 primitive types in C
		i.e int, float, char, long, uint		
	*/
	unsigned long int age;
	
	printf("Enter a number: ");
	scanf("%d", &age);
	
	printf("You are %u", age);
}
